<% if current_user %>
  <p>Bienvenido <%= current_user.username %></p>
  <%= link_to "Cerrar sesion", destroy_user_session_path, data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %>
  <p>Tu rol es [ <%= current_user.role %> ]</p>
<% else %>
  <%= link_to "Inicia sesión", new_user_session_path%>
  <%= link_to "Crea una cuenta!", new_user_registration_path%>
<% end %>